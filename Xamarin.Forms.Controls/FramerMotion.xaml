<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:l="clr-namespace:Xamarin.Forms.Controls"
             x:Class="Xamarin.Forms.Controls.FramerMotion">
    <FlexLayout Direction="Column">
        <FlexLayout FlexLayout.Grow="1">
            <AbsoluteLayout BackgroundColor="Purple"
                            FlexLayout.Grow="1">
                <l:Div x:Name="boxView">
                    <l:Div.Resources>
                        <Storyboard x:Key="animateScale">
                            <DoubleAnimation To="{Binding SliderValue}"
                                             Storyboard.TargetProperty="BoxView.Scale"></DoubleAnimation>
                            <DoubleAnimation To="{Binding Rotation}"
                                             Storyboard.TargetProperty="BoxView.Rotation"></DoubleAnimation>
                        </Storyboard>
                    </l:Div.Resources>

                </l:Div>
            </AbsoluteLayout>
            <FlexLayout
                FlexLayout.Basis="300"
                FlexLayout.Order="-1"
                BackgroundColor="#151515"
                Direction="Column"
                AlignItems="Center"
                JustifyContent="Start">
                <Label TextColor="White"
                       Margin="20"
                       Text="{Binding Source={x:Reference slider},
                              Path=Value,
                              StringFormat='Scale: {0:F0}'}" />
                <Stepper
                    Value="{Binding Source={x:Reference Name=slider}, Path=Value}" />
                <Slider x:Name="slider"
                        Maximum="5"
                        MaximumTrackColor="#909090"
                        Minimum="1"
                        Value="{Binding SliderValue, Mode=TwoWay}" />
                <Label TextColor="White"
                       Margin="20"
                       Text="{Binding Source={x:Reference sliderRotation},
                              Path=Value,
                              StringFormat='Rotation {0:F0}'}"
                       HorizontalOptions="FillAndExpand" />
                <Stepper Value="{Binding Source={x:Reference Name=sliderRotation}, Path=Value}" />
                <Slider x:Name="sliderRotation"
                        Margin="0,5,0,0"
                        Maximum="360"
                        Minimum="0"
                        Value="{Binding Rotation, Mode=TwoWay}" />
            </FlexLayout>

        </FlexLayout>
    </FlexLayout>
</ContentPage>